# To create or update a token visit https://envio.dev/app/api-tokens
# Required: Monad testnet's public RPC caps eth_getLogs at a 100-block range, so backfilling
# anything beyond the last ~100 blocks needs HyperSync, which needs this token.
ENVIO_API_TOKEN="<YOUR-API-TOKEN>"
